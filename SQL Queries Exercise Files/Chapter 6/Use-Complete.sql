USE AdventureWorks2019;
GO

-- GO needs to be added as the only command on the line, do not use ;
-- Some SQL commands need to be isolated, such as CREATE SCHEMA,
--   so place GO commands on either side to ensure proper execution in a script.

SELECT *
FROM Person.Person;